# Business Intelligence System

Flow:
1. raw/ = messy notes, imports
2. atoms/ = normalized business atoms (manual/auto)
3. prompts/ = templates to build docs
4. views/ = generated docs (business plan, budget, etc.)
5. deps.yaml = defines what to build from what

Build order:
raw → atoms → views

